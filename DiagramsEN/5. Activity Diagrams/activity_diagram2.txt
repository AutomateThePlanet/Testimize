@startuml
skinparam ActivityShape rounded
skinparam ActivityBorderColor #333333
skinparam ActivityBackgroundColor #E0E0E0
skinparam PartitionBorderColor #333333
skinparam ArrowColor #333333

start

:Generate initial population using PairwiseTestCaseGenerator;

:Evaluate population via fitness function;

repeat
    :Evaluate current population;
    
    :Select elite population;
    
    :Form non-elite population as evaluated - elite;
    
    if (EnableOnlookerSelection?) then (yes)
        :Apply onlooker selection with RCL mechanism;
        :Update non-elite based on probability selection;
    else (no)
        :Non-elite remains unchanged;
    endif
    
    :Apply mutation to non-elite population;
    
    if (EnableScoutPhase and stagnation detected?) then (yes)
        :Execute scout phase;
        :Replace stagnant solutions with new values;
    else (no)
        :Continue with current population;
    endif
    
repeat while (currentGeneration < TotalGenerations?)

:Apply FinalPopulationSelectionRatio;

:Return optimized test suite;

stop
@enduml