@startuml

' === Визуални настройки ===
skinparam shadowing false
skinparam roundcorner 0
skinparam nodesep 60
skinparam ranksep 40
left to right direction

' === Участници с "име : Клас" ===
rectangle "<u>generator : HybridArtificialBeeColonyTestCaseGenerator</u>" as generator
rectangle "<u>evaluator : ITestCaseEvaluator</u>" as evaluator
rectangle "<u>randomizer : Random</u>" as randomizer

' === Стартиране на алгоритъма ===
generator --> generator : 1. CreateInitialPopulation()
generator --> evaluator : 2. EvaluatePopulation(initialPopulation)

' === Итеративен цикъл ===
generator --> randomizer : 3.1 Select test cases for mutation
generator --> generator : 3.2 ApplyMutation(testCases)
generator --> evaluator : 3.3 EvaluatePopulation(mutatedPopulation)
generator --> generator : 3.4 CheckImprovement()

' === Условен блок за подобрение ===
generator --> generator : 3.5.1 IncreaseStagnationCounter() [ако няма подобрение]
generator --> generator : 3.5.2 UpdateBestPopulation() [ако има подобрение]

' === Финална селекция ===
generator --> generator : 4. SelectFinalPopulation()

@enduml
